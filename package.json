{
  "name": "agri-advisor",
  "version": "1.0.0",
  "description": "Agricultural Advisory System - crop advice for farmers",
  "main": "server.js",
  "scripts": {
    "start": "npm run migrate && node server.js",
    "migrate": "node db/migrations/run.js",
    "build": "cd web && npm run build",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
